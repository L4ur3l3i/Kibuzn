{% extends 'layout.html.twig' %}

    {% block title %}Edit Recurring Transaction
    {% endblock %}
    
    {% block content %}
    
    
        <div class="sm:flex sm:items-center">
    
            <div class="sm:flex-auto">
                {% include '_elements/_back_button.html.twig' with {'to': 'Recurring Transactions', 'route': path('app_recurring_transaction_index')} %}
                <h1 class="text-base font-semibold leading-6 text-gray-900">Edit Recurring Transaction</h1>
            </div>
    
        </div>
    
        {{ include('recurring_transaction/_form.html.twig') }}
    
    {# {{ include('recurring_transaction/_delete_form.html.twig') }} #}
    
    {% endblock %}