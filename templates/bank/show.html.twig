{% extends 'admin.html.twig' %}

    {% block html_class %}h-full bg-gray-100
    {% endblock %}
    {% block body_class %}h-full
    {% endblock %}

{% block title %}Bank{% endblock %}

{% block content %}
    <h1>Bank</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ bank.id }}</td>
            </tr>
            <tr>
                <th>Brand</th>
                <td>{{ bank.brand }}</td>
            </tr>
            <tr>
                <th>Logo</th>
                <td>{{ bank.logo }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_bank_index') }}">back to list</a>

    <a href="{{ path('admin_bank_edit', {'id': bank.id}) }}">edit</a>

    {{ include('bank/_delete_form.html.twig') }}
{% endblock %}
